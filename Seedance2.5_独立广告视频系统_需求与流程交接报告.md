# Seedance 2.5 独立广告视频系统：需求、业务流程与交接报告

> 文档版本：0.1
> 整理日期：2026-08-11
> 当前阶段：需求确认与流程梳理，尚未进入系统设计、技术架构定稿或开发
> 使用目的：即使更换产品经理、开发人员或 AI，也可以仅阅读本报告继续后续讨论，不依赖原聊天记录
> 保密要求：本文不保存任何真实 API Key

---

## 1. 报告状态与阅读规则

本文只记录以下内容：

1. 用户已经明确确认的产品目标和限制。
2. 三个页面的完整业务流程。
3. 视频理解、素材理解、提示词生成和结果验收的业务原则。
4. 用户提供的 Seedance 2.5 官方直连与 Comfly 接口契约。
5. 已知风险、当前工作假设和后续待确认问题。
6. 后续 AI 或开发人员继续工作的顺序和禁止误解事项。

本文**不是**以下文档：

- 不是 UI 设计稿。
- 不是数据库设计。
- 不是技术架构定稿。
- 不是开发排期。
- 不是可直接执行的代码实施计划。
- 不代表已经验证所有上游接口在当前日期仍然有效。

文中使用以下状态标签：

- **已确认**：用户已经明确同意，不应由后续 AI 擅自改变。
- **接口文档声明**：来自用户提供的配置文件，实施时仍应通过真实接口验证。
- **工作假设**：目前合理，但需要实验或用户再次确认。
- **待决定**：尚未确定，后续必须逐项询问或测试。

---

## 2. 项目一句话定义

**已确认：**建设一个完全独立、部署在用户自有服务器上的广告视频生成系统，供公司内部人员使用；系统以 Seedance 2.5 为视频生成模型，通过视觉 AI 理解参考视频、人物图片、产品图片和背景图片，先生成可编辑的结构化提示词，再完成广告视频生成。

系统未来可能商用，因此从一开始必须重视：

- 项目和数据可迁移。
- 接口供应商可替换。
- API Key 安全。
- 任务、成本和结果可追溯。
- 用户输入、AI观察、AI推断和用户修改能够区分。
- 生成结果有明确的合格与不合格标准。

第一阶段只面向公司内部人员，**暂不增加新的账号注册、登录、权限和计费功能**。未来商用能力不得反向破坏当前内部使用的简单流程。

---

## 3. 独立系统边界

### 3.1 必须独立

**已确认：**这是一个全新系统，不依赖 `D:\ai工具` 中的旧项目运行。

新系统应拥有自己的：

- 代码目录和代码仓库。
- 前端页面。
- 后端服务。
- 视频和图片素材目录。
- 项目数据。
- 任务状态记录。
- 模型配置。
- API Key 配置。
- 日志、错误记录和结果文件。
- 部署与启动方式。

### 3.2 旧项目只能参考

可以阅读和参考旧项目中以下经验：

- FFmpeg 视频元数据读取。
- 镜头候选边界识别与切点校准思路。
- 关键帧选择方法。
- 视觉逆向分镜制作台的结构化输出方式。
- 单镜头分析提示词。
- 任务轮询、超时恢复和结果保存经验。

但新系统不得形成以下依赖：

- 不直接导入旧项目模块。
- 不调用旧项目服务才能运行。
- 不共用旧项目数据库或运行数据目录。
- 不要求旧项目先启动。
- 不把旧项目页面嵌入新系统。

如果复用代码，应在确认权属、许可证和适用性后，将必要逻辑整理为新系统自己的实现和测试，而不是保留隐式耦合。

---

## 4. 三个页面与三个业务 Agent

**已确认：**必须是三个彼此独立的页面，也可以把它们理解成三个有固定步骤、固定输入输出和人工确认门槛的流程 Agent。它们不是三个随意聊天的 AI，而是三个可重复执行、可追踪的业务工作流。

### 4.1 页面一：保持原产品的参考视频改编

目标：上传想复刻的广告视频，保持视频中的产品不变，允许修改人物、背景或分镜内容。

支持的两类修改：

1. 保持产品和主要分镜，更换人物或背景。
2. 保持产品和广告结构，调整某些镜头，使新视频与原视频相似但不完全相同。

关键原则：

- 原视频中的产品是锁定资产。
- 用户不允许在该页面把产品改成另一个产品。
- 未被用户明确修改的内容应尽量保持。
- 最终提交给 Seedance 的是完整参考视频和按时间段组织的修改提示词。

### 4.2 页面二：替换产品的参考视频改编

目标：上传想复刻的广告视频，再上传新的目标产品；替换原视频中的产品，同时允许修改人物、背景或分镜内容。

支持的两类修改：

1. 替换产品，并更换人物或背景。
2. 替换产品，并修改部分镜头结构或动作，使其保持原广告逻辑但不完全相同。

关键原则：

- 新产品必须先被视觉 AI 分析为详细的产品文字档案。
- 原视频中的产品动作不能机械套用到不兼容的新产品。
- 产品形态、开口方式、使用方式和动作必须匹配。
- 页面二是当前最大技术风险，因为成功的人物/背景替换案例不能自动证明新产品能够被精确替换。

### 4.3 页面三：从用户思路生成完整分镜

目标：用户输入广告思路、分镜词、产品信息和可选的人物/背景信息，系统通过内置 AI 补全和优化，生成可提交给 Seedance 2.5 的完整分镜提示词。

关键原则：

- 页面三不依赖参考视频。
- 页面三不是生成“修改指令”，而是生成从 0 秒开始的完整广告视频提示词。
- 用户可以在提交生成前查看、修改和确认每个时间段。
- AI 应补全动作、运镜、节奏、场景、人物、产品状态、连续性和禁止项。

---

## 5. 页面一和页面二的完整流程

### 5.1 阶段 A：创建任务与上传参考视频

用户进入页面一或页面二后：

1. 上传完整广告视频或用户已经准备好的镜头视频。
2. 系统读取文件格式、时长、帧率、分辨率、方向和音轨信息。
3. 系统检查文件是否损坏、是否可解码、是否满足后续 Seedance 参考视频限制。
4. 保存原始文件，不对原视频做不可逆覆盖。
5. 页面二另外要求上传目标产品图片；人物和背景图片根据用户修改需求选填。

**已确认：**用户上传的新视频通常不是一镜到底。系统不得为了方便生成而擅自把它改成一镜到底，也不得为了方便处理而凭空添加切镜。

### 5.2 阶段 B：完整视频理解

系统首先理解完整视频，而不是立即只看关键帧。

完整视频理解应得到：

- 广告整体在表达什么。
- 开场钩子、展示段、使用段、效果段和收尾段的关系。
- 整体节奏快慢。
- 同一个人物、产品和场景如何跨镜头延续。
- 产品首次出现时间、核心展示时间和使用时间。
- 镜头之间的因果、对比或连续关系。
- 是否存在字幕、口播、背景音乐和明显音效。

完整视频理解的职责是建立全局语境，不负责独自决定精确切点。

### 5.3 阶段 C：真实镜头边界与时间轴

切镜识别不是产品核心价值，但它是生成时间段提示词的必要工具。

推荐业务原则：

1. FFmpeg 或等价的本地视频工具负责获取帧时间戳、画面差异和候选切点。
2. 视觉 AI 根据场景、主体、机位、动作连续性和叙事语义判断候选切点是否是真实镜头边界。
3. 精确时间来自视频帧和本地工具，不允许由语言模型凭空编造。
4. 用户可以查看和修正边界。
5. 镜头切分只用于分析和提示词时间范围，不等于把每个镜头分别提交给 Seedance 生成。

### 5.4 阶段 D：逐镜头视频理解

对每一个真实镜头，应把**完整镜头视频片段**交给支持视频输入的视觉模型理解。

每个镜头至少识别：

- 镜头目的：开场、人物展示、产品特写、产品使用、效果展示、情绪建立或收尾。
- 主体：人物、手、产品、道具和背景。
- 人物外貌、服装、发型、妆容和表情。
- 动作的完整过程：开始状态、连续过程、结束状态。
- 产品与手、嘴、桌面、容器和其他道具的关系。
- 景别、机位、构图和主体位置。
- 推、拉、摇、移、跟随、固定、环绕、变焦等运镜。
- 光线、色温、景深、画质和商业摄影风格。
- 字幕、包装文字、Logo 和可见标识。
- 遮挡、运动模糊和无法确认的部分。

不能只凭静态帧判断“拿起还是放下”“挤出还是收回”“倒入还是倒出”等有方向的动作。

### 5.5 阶段 E：关键帧与视觉证据

关键帧是辅助证据，不是视频理解的替代品。

每个镜头应保留：

- 开始状态帧。
- 动作中间状态帧。
- 结束状态帧。
- 产品最清晰的帧。
- 人物最清晰的帧。
- 背景最完整的帧。
- 字幕、Logo 或包装文字最清晰的帧。
- 画面变化明显或模型判断不确定的帧。

关键帧主要用于：

- 确认人物外观。
- 确认产品包装和颜色。
- 确认背景细节和构图。
- 识别文字。
- 给用户展示 AI 判断依据。
- 对生成结果做前后对比。

### 5.6 阶段 F：合并为“原视频事实时间轴”

完整视频理解、逐镜头视频理解和关键帧证据需要合并为统一结果。概念结构如下：

```json
{
  "videoSummary": {
    "purpose": "广告目标",
    "story": "整体内容",
    "rhythm": "整体节奏",
    "globalStyle": "整体视觉风格"
  },
  "entities": {
    "characters": [],
    "products": [],
    "scenes": []
  },
  "shots": [
    {
      "startSec": 0,
      "endSec": 3,
      "purpose": "镜头作用",
      "character": {},
      "actionSequence": [],
      "product": {},
      "productInteraction": {},
      "background": {},
      "camera": {},
      "lighting": {},
      "visibleText": [],
      "evidenceFrames": [],
      "observations": [],
      "inferences": [],
      "uncertainties": []
    }
  ]
}
```

必须明确区分：

- 视频中确实看见的事实。
- AI 根据连续动作做出的推断。
- AI 无法确认的内容。
- 用户明确纠正或修改的内容。

### 5.7 阶段 G：向用户展示原视频提示词

系统把事实时间轴转换成用户可以理解和编辑的提示词视图。用户应能按时间段看到：

- 背景。
- 人物外貌和服装。
- 人物动作。
- 产品外观和状态。
- 产品使用动作。
- 运镜和构图。
- 光线和画质。
- 需要保持的内容。
- AI 不确定、需要人工确认的内容。

页面一和页面二的核心不是让用户面对空白输入框，而是让用户在 AI 已经生成的原视频描述上挑出要修改的地方。

### 5.8 阶段 H：分析新增人物、产品和背景素材

用户上传人物、产品或背景图片后，不应默认把所有图片直接交给 Seedance。

**已确认的当前策略：**先用独立视觉模型把参考素材转换成详细、可编辑的文字档案，用户确认后再参与提示词生成。

人物档案至少包括：

- 年龄段和可见性别表达。
- 面部轮廓和五官特征。
- 肤色、妆容和表情。
- 发色、发型、长度和碎发。
- 身材和体态。
- 服装类别、颜色、材质和版型。
- 饰品和不可擅自推断的身份信息。

产品档案至少包括：

- 产品类别和用途。
- 整体形状和尺寸比例。
- 包装结构、开口、盖子和密封方式。
- 主色、辅色、材质和表面反光。
- Logo 位置、包装文字和图案区域。
- 正面、侧面、背面和使用状态。
- 与手部互动的合理姿势。
- 绝对不能变化的属性。

背景档案至少包括：

- 空间类型和时代风格。
- 桌面、墙面、窗户和主要陈设。
- 前景、中景和背景层次。
- 主光方向、色温、柔硬程度和阴影。
- 景深、虚化和氛围。
- 不希望出现的物体。

用户已经观察到：同时把人物图片、产品图片等多个参考素材直接提交给 Seedance 2.5，模型可能理解不佳；把外观描述得足够详细时，人物和背景替换已有成功案例。因此，素材转文字是当前主路径；是否在特定任务中同时提交原图，应作为后续 A/B 实验项，而不是默认行为。

### 5.9 阶段 I：用户修改与差异记录

页面一和页面二都应记录：

- 修改发生在哪个时间段。
- 修改对象是人物、产品、背景、动作、运镜还是完整镜头。
- 修改前内容。
- 修改后内容。
- 哪些内容必须保持不变。
- 修改是否会影响其他镜头的连续性。

页面一必须阻止产品字段被替换。

页面二必须把新产品应用到所有相关镜头，并根据新产品形态检查动作是否合理。

### 5.10 阶段 J：生成最终差异提示词

页面一和页面二最终生成的不是无差别重写整条视频，而是：

```text
原视频事实
+ 用户修改清单
+ 保持不变清单
+ 产品和人物一致性约束
+ 画质要求
+ 禁止项
= 最终按时间段编写的 Seedance 修改提示词
```

推荐的提示词语义顺序：

1. 明确“参考视频 1 的完整分镜、动作顺序、运镜和节奏”。
2. 写全局画质和风格要求。
3. 按真实时间段写需要修改的内容。
4. 每个时间段明确需要保持的动作、产品、构图或其他元素。
5. 写跨镜头一致性要求。
6. 写禁止出现的错误。

### 5.11 阶段 K：用户最终确认

提交 Seedance 前，用户至少确认：

- 时间段是否正确。
- 人物描述是否符合目标。
- 产品描述和文字是否正确。
- 背景是否符合目标。
- 哪些动作和运镜必须保持。
- 是否存在互相冲突的指令。
- 当前选择的生成供应商。
- 是否生成音频、水印和返回尾帧。

### 5.12 阶段 L：提交 Seedance 2.5

页面一和页面二：

- 提交最终文字提示词。
- 提交完整参考视频，角色为 `reference_video`。
- 有参考视频时强制使用 `ratio: adaptive` 和 `duration: -1`。
- 不把分析阶段的镜头切片当成多个独立 Seedance 任务，除非用户后续明确选择逐镜生成方案。

页面三：

- 提交完整原创分镜提示词。
- 根据后续确认结果决定是否同时提供图片、首帧、尾帧或音频参考。
- 无参考视频时使用用户选择的固定时长与画幅。

### 5.13 阶段 M：任务追踪、结果保存与质检

创建成功后必须立即保存任务 ID，不得等待视频完成才记录。

完整过程：

```text
提交任务
→ 保存供应商、模型、请求摘要、任务 ID 和创建时间
→ 查询 queued/running/processing
→ 页面关闭或服务重启后恢复查询
→ succeeded 后取得视频 URL
→ 尽快下载或转存结果
→ 运行视频与产品一致性质检
→ 标记合格或不合格
```

前端等待超时不代表上游任务失败。未知创建结果时不得立即重复提交，以免重复扣费。

---

## 6. 页面三的完整流程

### 6.1 用户输入

用户至少提供：

- 想生成什么广告。
- 产品名称、类型或产品图片。
- 核心卖点或希望表现的效果。
- 目标时长和画幅。

可选输入：

- 人物图片或人物描述。
- 背景图片或背景描述。
- 目标受众。
- 平台风格。
- 台词、旁白或音效方向。
- 必须出现和禁止出现的内容。

### 6.2 素材分析

人物、产品和背景图片先经过视觉理解，形成与页面一、页面二相同的文字档案。用户可以修正识别错误。

### 6.3 创意补全

AI 根据用户思路补全：

- 广告开场钩子。
- 镜头顺序。
- 人物和产品动作。
- 运镜与构图。
- 光线、背景和商业质感。
- 产品出现位置和展示时长。
- 连续性约束。
- 负面提示词。

AI 补全的内容必须显示给用户，不能冒充用户原始要求。

### 6.4 动作与产品形态匹配

当前已经提出并需要优先覆盖的产品动作：

1. 罐装或广口容器产品：打开后用手取出、抠出或挖取其中内容物。
2. 盒装饮品，例如牛奶：拿起、靠近嘴部、饮用、放下。
3. 牙膏式软管产品：打开盖子、挤压外壳、内容物从管口挤出。

后续可以扩展更多产品类型，但不能在需求尚未出现时构建庞大而未经验证的动作库。

### 6.5 分镜生成与检查

每个分镜至少包含：

- 起止时间。
- 镜头目的。
- 景别、机位和运镜。
- 人物和产品位置。
- 动作开始、过程和结束。
- 产品状态变化。
- 背景、光线和构图。
- 台词、声音或无声音要求。
- 与前后镜头的连续关系。
- 禁止项。

生成前检查：

- 时间段首尾相接且覆盖全片。
- 动作数量与时长匹配。
- 同一人物和产品描述一致。
- 产品使用动作合理。
- 同一镜头没有互相冲突的运镜。
- 没有虚构产品功效、包装文字或人物身份。

### 6.6 用户修改与提交

用户可以修改单个镜头，而不必重新生成全部方案。确认后转换为完整 Seedance 提示词，并进入与页面一、页面二相同的提交、轮询、保存和质检流程。

---

## 7. 产品一致性与合格标准

### 7.1 产品硬性标准

**已确认：**如果生成结果中任意可见帧出现以下错误，直接判定不合格：

- Logo 错误、变形、消失或漂移。
- 包装文字错误、乱码或被重写。
- 主色或关键配色错误。
- 产品结构、开口、盖子、比例或形状错误。
- 产品在镜头间变成不同版本。
- 产品使用方式与真实结构不符。

如果系统无法确认关键产品属性是否正确，也应进入不合格或待复核状态，不得自动宣称合格。用户此前明确倾向于“直接判断不合格”。

### 7.2 其他质量检查

至少检查：

- 是否遵循参考视频的镜头顺序和时间结构。
- 人物是否在镜头之间漂移。
- 动作方向和过程是否正确。
- 运镜是否符合提示词。
- 手部、嘴部和产品互动是否变形。
- 是否多出人物、手指、产品或道具。
- 是否存在黑帧、闪烁、严重模糊和突然跳变。
- 背景是否按要求修改或保持。
- 画面是否出现未经要求的字幕、水印或文字。

### 7.3 对生成能力的现实边界

Seedance 2.5 当前被当作直接生成带产品视频的首选路径，而不是局部编辑工具。生成模型无法保证每次都精确还原产品包装，因此：

- 系统必须能严格拒绝错误结果。
- 不得对用户承诺逐帧完全一致。
- 页面二必须通过真实样例验证新产品替换的可行率。
- 如果连续多次生成仍无法满足产品硬性标准，可考虑把真实产品合成到生成视频中的备用路线；用户当前更倾向直接生成带产品的视频，备用路线不能取代主路径。

---

## 8. Seedance 2.5 双供应商接口契约

以下内容来自用户于 2026-08-11 提供的两份配置文件。实施时不得擅自添加未出现在文档或真实接口验证中的参数。

### 8.1 官方直连版

```json
{
  "name": "Doubao Seedance 2.5",
  "provider": "volcengine",
  "apiBase": "https://ark.cn-beijing.volces.com",
  "apiModel": "doubao-seedance-2-5-260628",
  "submitPath": "/api/v3/contents/generations/tasks",
  "statusPathPrefix": "/api/v3/contents/generations/tasks/"
}
```

创建任务：

```http
POST https://ark.cn-beijing.volces.com/api/v3/contents/generations/tasks
```

查询任务：

```http
GET https://ark.cn-beijing.volces.com/api/v3/contents/generations/tasks/{task_id}
```

鉴权：

```http
Authorization: Bearer YOUR_ARK_API_KEY
Content-Type: application/json
```

官方配置文档声明的主要能力：

- 支持音频生成。
- 支持返回尾帧。
- 支持首帧、尾帧、多图片、参考图片、参考视频和参考音频。
- 支持水印和分辨率。
- 参考视频时长 4–30 秒。
- 生成时长 4–30 秒。
- 前端接入配置显示 480p、720p。
- 参考视频模式要求自适应画幅。
- 最多 30 张参考图片、10 个参考视频、10 个参考音频；这些是来源系统的前端接入配置，实际服务端限制仍需验证。
- 当前配置不发送 `camera_fixed` 和 `draft`。

官方来源系统的轮询配置是每 2 秒查询、前端最多等待 15 分钟，但文档明确指出前端超时不等于任务失败。

来源系统使用过以下本地设置键：

```text
tapnow-api-base
tapnow-api-key
```

这些名称只用于记录原实现兼容信息。新系统是否沿用属于后续设计问题；无论是否沿用，Key 都不能下发到普通浏览器前端。

真人素材可能触发可信素材和肖像授权检查。需要真人素材时，应按火山方舟规则获取和引用可用 Asset ID。不得把规避授权或内容安全作为系统功能。

### 8.2 Comfly 版

```json
{
  "provider": "comfly",
  "name": "Seedance 2.5（Comfly版）",
  "apiBase": "https://ai.comfly.chat",
  "apiModel": "doubao-seedance-2.5",
  "submitPath": "/seedance/v3/contents/generations/tasks",
  "statusPathPrefix": "/seedance/v3/contents/generations/tasks/",
  "pollIntervalMs": 5000,
  "maxWaitMs": 2700000
}
```

创建任务：

```http
POST https://ai.comfly.chat/seedance/v3/contents/generations/tasks
```

查询任务：

```http
GET https://ai.comfly.chat/seedance/v3/contents/generations/tasks/{task_id}
```

鉴权：

```http
Authorization: Bearer YOUR_COMFLY_API_KEY
Content-Type: application/json
```

Comfly 文档声明：

- 参考视频时长 4–30 秒。
- 参考视频模式必须使用 `ratio: adaptive` 和 `duration: -1`。
- 默认建议 720p。
- 控制台还显示 1080p、2K、4K，其中 2K/4K 被标注为 720p upscale，不应宣称为原生生成。
- 部分任务可能超过 20 分钟，推荐最大等待 45 分钟。
- 推荐分段轮询：0–2 分钟每 2 秒，2–10 分钟每 5 秒，10–45 分钟每 10 秒。

来源文档使用以下服务端环境变量名称：

```text
COMFLY_API_BASE
COMFLY_API_KEY
COMFLY_SEEDANCE_MODEL
```

用户反馈 Comfly 在含人脸素材的任务中阻拦相对少。该反馈只记录为用户实际观察，不能据此承诺所有真人任务都会成功，也不能把 Comfly 定义为绕过内容安全的渠道。所有真人、肖像和商标素材仍需具备合法授权。

### 8.3 两个供应商的共同请求体

```json
{
  "model": "供应商对应的模型名称",
  "content": [],
  "duration": 5,
  "ratio": "16:9",
  "resolution": "720p",
  "seed": -1,
  "return_last_frame": false,
  "watermark": false,
  "generate_audio": true,
  "execution_expires_after": 86400
}
```

文字输入：

```json
{
  "type": "text",
  "text": "保持视频1的动作与运镜，按时间段修改人物和背景。"
}
```

首帧：

```json
{
  "type": "image_url",
  "image_url": { "url": "https://example.com/first-frame.jpg" },
  "role": "first_frame"
}
```

尾帧：

```json
{
  "type": "image_url",
  "image_url": { "url": "https://example.com/last-frame.jpg" },
  "role": "last_frame"
}
```

参考图片：

```json
{
  "type": "image_url",
  "image_url": { "url": "https://example.com/reference.jpg" },
  "role": "reference_image"
}
```

参考视频：

```json
{
  "type": "video_url",
  "video_url": { "url": "https://example.com/reference.mp4" },
  "role": "reference_video"
}
```

参考音频：

```json
{
  "type": "audio_url",
  "audio_url": { "url": "https://example.com/reference.mp3" },
  "role": "reference_audio"
}
```

本地文件不能直接使用 `file://`、`blob:` 或仅局域网可访问的地址。图片、视频和音频必须先成为上游服务能够访问的公网 URL。公网素材地址如何提供属于后续系统设计中的关键问题，本文暂不定方案。

### 8.4 参考视频模式强制规则

只要 `content` 中存在 `reference_video`，请求必须改成：

```json
{
  "ratio": "adaptive",
  "duration": -1
}
```

已知错误包括：

```text
ratio must be adaptive
duration must be -1
video duration must be between 4 and 30 seconds
```

因此，页面一和页面二不能继续让用户为参考视频任务强行指定固定输出时长或固定画幅。系统可以显示源视频信息，但最终提交参数必须满足该规则。

### 8.5 创建任务响应

供应商可能返回：

```json
{ "id": "TASK_ID" }
```

也可能返回：

```json
{ "task_id": "TASK_ID" }
```

两种字段都必须兼容。取得任务 ID 后立即持久化：

```json
{
  "provider": "volcengine 或 comfly",
  "model": "实际模型名称",
  "taskId": "TASK_ID",
  "status": "submitted",
  "createdAt": "创建时间"
}
```

不得在项目数据、日志或错误信息中保存 API Key。

### 8.6 查询状态与成功结果

需要兼容并忽略大小写的状态：

```text
queued
running
processing
succeeded
failed
cancelled
expired
```

成功视频地址至少需要兼容以下常见位置：

```text
content.video_url
data.video_url
video_url
url
result.video_url
result.url
content.content.video_url
data.data.video_url
数组第一项的 video_url 或 url
```

成功后应保存原始 URL，并尽快下载或转存，避免临时地址失效。

Comfly 成功响应还可能包含 `credits`、`duration` 和 `usage`。这些字段如果实际返回，应原样记录为任务元数据，但不能在没有正式计费定义时擅自解释 `credits` 的币种或费用含义。

### 8.7 轮询与重试规则

- 每次 HTTP 请求都必须有独立超时。
- 429、500、502、503 可以按退避策略重试查询。
- 创建任务请求结果不明确时，不能盲目重复创建。
- 前端超时不能删除任务。
- 页面刷新和服务重启后应能恢复查询。
- 应提供使用任务 ID 重新查询的能力。
- 防止重复点击造成重复任务和重复扣费。

### 8.8 API 契约验证的准确含义

两份文档已经给出了主要请求体，所以后续所说的“打通接口”不再是猜测格式，而是上线前使用已获授权、成本较低的测试素材实际验证：

1. 官方和 Comfly 是否接受文档中的请求体。
2. 公网素材 URL 的访问要求。
3. 参考视频模式是否确实要求 `adaptive/-1`。
4. 两家返回的任务 ID、状态和视频 URL 实际位于什么字段。
5. 实际允许的时长、分辨率、素材数量和文件大小。
6. 长任务、限流和失败响应的真实格式。
7. 真人授权素材在官方通道中的实际使用方式。

测试素材时长较短只为降低首次接口验证成本，不代表产品只支持短视频，也不代表质量验收只使用短视频。

---

## 9. API Key、安全与合规要求

**已确认：**禁止把 API Key 硬编码到以下位置：

- 浏览器前端代码。
- Git 仓库。
- 安装包。
- 公开文档。
- 客户端日志。
- 项目导出文件。

前端只允许选择已配置的供应商或查看“是否已配置”，不能获得完整 Key。

系统未来可能商用，需保留以下业务要求，但当前不展开具体设计：

- 每次生成记录供应商、模型、任务 ID、创建时间、状态、耗时和已知费用。
- 记录参考素材来源和授权确认。
- 不把 Comfly 作为绕过真人审核的产品卖点。
- 不允许未经授权复刻他人肖像、商标、广告或受保护内容。
- 失败日志需要可诊断，但必须清除密钥和不必要的隐私信息。

---

## 10. 必须保存的业务过程数据

这是业务连续性要求，不是数据库方案。无论未来采用何种技术，都应能够恢复：

- 项目标识、项目名称和页面模式。
- 原始参考视频及其元数据。
- 用户上传的人物、产品和背景素材。
- 完整视频理解结果。
- 镜头边界和逐镜理解结果。
- 关键帧及其时间戳。
- 人物、产品和背景文字档案。
- AI 观察、AI 推断、不确定项和用户纠正。
- 原视频提示词版本。
- 用户修改记录。
- 最终提交提示词版本。
- 实际提交给供应商的非敏感请求摘要。
- 供应商、模型、任务 ID、状态和错误。
- 结果视频 URL、本地结果和生成时间。
- 质量检查结果和不合格原因。
- 重新生成与版本关系。

---

## 11. 失败场景与业务处理原则

### 11.1 上传失败

- 文件损坏或格式不支持：阻止进入分析。
- 文件超过系统限制：明确提示，不静默压缩原文件。
- 上游无法访问素材 URL：保留本地素材并允许重新发布地址。

### 11.2 视频分析失败

- 完整视频模型失败时，不得伪装成已理解。
- 可以保留 FFmpeg 时间信息和关键帧证据，但必须标记分析不完整。
- 单个镜头失败时允许只重试该镜头。
- AI 输出无法解析或互相矛盾时进入人工复核。

### 11.3 提示词失败

- 时间段重叠、遗漏或超出视频时长：不允许提交。
- 页面一出现产品替换指令：不允许提交。
- 页面二产品动作与包装结构冲突：不允许提交或要求用户确认修改动作。
- 同一人物、产品或背景在不同段描述冲突：先修复再提交。

### 11.4 Seedance 创建失败

- 401：配置或密钥问题，不能自动换供应商掩盖。
- 429：等待后重试，避免并发重复创建。
- 500/502/503：在确认没有创建成功后再重试。
- 返回成功但没有任务 ID：保存原响应摘要并标记人工处理。
- 请求是否创建成功不明确：优先查找任务，不直接重新扣费。

### 11.5 轮询超时

- 本地等待超时不代表上游失败。
- 保留任务 ID 和最后状态。
- 允许稍后恢复查询或手动输入任务 ID。

### 11.6 生成结果不合格

- 明确列出失败时间段、失败对象和证据帧。
- 用户可以修改提示词后生成新版本。
- 新版本不得覆盖旧结果。
- 产品硬性属性错误直接不合格。

---

## 12. 当前已知限制与风险

1. Seedance 2.5 更接近基于参考内容重新生成，不是传统局部视频编辑工具。
2. 多个原始参考图片与参考视频同时输入时，模型可能混淆；用户已有此类体验。
3. 人物和背景的详细文字替换已经有成功案例，但新产品精确替换仍需专项验证。
4. 自动视觉质检无法提供数学意义上的逐帧绝对保证，只能采取严格拒绝和人工复核。
5. 官方参考视频限制为 4–30 秒。超过 30 秒的视频不能直接按当前文档作为单个参考视频提交；如何处理必须由用户后续决定，不能擅自分段生成。
6. 本地素材需要被 Seedance 供应商访问，意味着系统必须解决安全的公网素材 URL 和生命周期问题。
7. 官方真人素材可能要求 Asset ID；具体授权流程仍需以正式控制台为准。
8. Comfly 的实际稳定性、费用、分辨率和安全政策需要真实测试和持续记录。
9. 上游模型、模型 ID、限制和响应结构未来可能变化，因此供应商契约必须可更新。

---

## 13. 当前明确不做的事项

- 暂不增加新的账号注册和账号设置页面。
- 暂不做计费、套餐和支付。
- 暂不承诺逐帧完全复刻。
- 暂不把镜头切分当作自动逐镜生成方案。
- 暂不把多个原始参考图片直接提交 Seedance 设为默认路径。
- 暂不把旧系统作为新系统运行依赖。
- 暂不确定最终前端框架、数据库、队列和对象存储。
- 暂不开始页面视觉设计或代码实现。

---

## 14. 后续必须逐项确认的问题

后续 AI 应一次只询问一个会影响设计的关键问题，不要一次抛出大量问题。

当前未决问题包括：

1. 完整视频和逐镜头视频由哪个视觉模型理解，是否需要可切换供应商。
2. 参考视频超过 30 秒时，是拒绝、要求用户裁剪，还是进入用户确认的分段方案。
3. 页面三默认生成音频还是默认静音。
4. 页面一和页面二是否允许用户选择同时把人物/产品参考图片提交给 Seedance，还是只允许文字档案。
5. 新产品图片要求上传几个角度，哪些角度是必填。
6. 产品包装文字和 Logo 的识别及质检需要多严格、是否引入专门 OCR。
7. 生成结果的“合格”是否必须人工最终确认，还是系统判定后即可进入可用库。
8. 内部第一阶段预计同时使用的人数和并发任务数。
9. 公网素材 URL 使用自建对象存储、云对象存储还是临时签名地址。
10. 是否需要保留原视频音频、重新生成音频或完全静音。
11. 技术栈此前提出过 React、FastAPI、PostgreSQL、独立 Worker、FFmpeg 和 Docker Compose，但本轮用户要求先不开始设计，因此该技术栈只作为候选，尚不在本文中锁定。

---

## 15. 后续工作推荐顺序

本文完成后，后续工作应按以下顺序继续：

1. 用户审阅并纠正本报告中的需求事实。
2. 逐项回答第 14 节中的关键未决问题。
3. 比较 2–3 套独立系统架构方案及取舍。
4. 分段确认系统架构、三个页面、数据流、错误处理和验收方案。
5. 把已确认设计写成独立设计规格。
6. 用户审核设计规格。
7. 再编写逐文件、逐任务的实施计划。
8. 最后才开始新建代码和开发。

不得跳过需求确认，直接依据旧项目开始编码。

---

## 16. 后续 AI 继续工作时必须遵守的说明

下一位 AI 应首先阅读本文，并遵守：

1. 不要再次建议在 `D:\ai工具` 的旧系统中扩展；这是一个全新独立系统。
2. 不要把三个页面重新解释成旧文档中的其他三个 Agent。
3. 页面一和页面二是基于参考视频事实提示词进行修改；页面三才是从思路生成。
4. 视频内容理解是核心，切镜仅服务于时间段和语义组织。
5. 完整视频、单镜头视频和关键帧必须各司其职，不能只看关键帧。
6. 页面一产品不可替换；页面二必须替换产品；页面三从用户思路创建。
7. API Key 只允许存在服务端安全配置。
8. 有参考视频时使用 `ratio: adaptive`、`duration: -1`，参考视频时长 4–30 秒。
9. 官方与 Comfly 的模型名称和接口路径不同，不能混用。
10. 产品 Logo、文字、颜色或结构出现错误时直接不合格。
11. Comfly 不得被描述为规避安全审核工具。
12. 在用户批准设计前，不创建项目代码。

---

## 17. 已验证成功的提示词模式

以下是用户已经实际成功使用的两类提示词经验。它们证明详细人物/背景描述和按时间段修改对 Seedance 有效，但不能单独证明新产品精确替换已经解决。

### 17.1 成功模式 A：按大时间段替换背景和人物

```text
保持视频1的完整分镜
提高视频清晰度
使用索尼 A7R5，100mm f/2.8 微距定焦镜头，光圈 f-8，快门速度 1/125s，ISO100，手动白平衡 5200K，点测光，单点精准对焦，RAW 格式输出，8K 超高清，16bit 色深，高动态范围 HDR，保留原生物体肌理，锐度适中，无过度磨皮，细节锐利干净，商业影棚布光，柔光箱主光 + 补光板 + 轮廓光，柔和均匀光影，低对比度，干净通透，色彩还原准确，中性灰平衡，浅景深可控，轻微自然虚化，无畸变，镜头色散抑制，商业电商质感
将0-12s的视频背景修改为轻奢梳妆台场景；白色丝绒质感桌面，背景点缀粉白渐变玫瑰仿真花艺，右侧金色托盘摆放多瓶精华小样，前景散落白色珍珠串链，左下角露出欧式金色雕花镜面边框，整体布置精致温柔，充满女性化氛围感。柔和暖白柔光均匀打亮产品，光线通透不刺眼，浅景深虚化背景突出主体。保持其他元素不变
12-19s将视频人物修改为年轻东亚女性，乌黑长发盘成利落丸子头，面容清爽干净，自然淡妆，神态平和端正。身着浅灰色大圆领短袖紧身保暖内衣套装，莫代尔亲肤面料，版型贴身修身、自然勾勒身形线条，搭配同色系紧身长裤，场景为室外。
19-24s视频背景修改为轻奢梳妆台场景；白色丝绒质感桌面，背景点缀粉白渐变玫瑰仿真花艺，右侧金色托盘摆放多瓶精华小样，前景散落白色珍珠串链，左下角露出欧式金色雕花镜面边框，整体布置精致温柔，充满女性化氛围感。柔和暖白柔光均匀打亮产品，光线通透不刺眼，浅景深虚化背景突出主体。保持其他元素不变
```

### 17.2 成功模式 B：按密集时间段替换人物和场景

```text
严格按照视频1的分镜进行处理。
将0-3秒女生换成20岁东亚精致女生，鹅蛋脸，浅棕色微卷中长发，几缕碎发散落脸颊，身穿米白色针织吊带
将3-4秒产品背景换成新中式禅意静物场景，前景是大块实木桌面，清晰自然的木头木纹肌理，柔和暖调斜向阳光，光线斜洒在木桌上，形成柔和光影；画面左后方虚化，摆放一盆小型盆景绿植、深色木托盘，米黄色哑光肌理墙面作为背景，浅景深，不改变动作和产品
将4-7秒女生换成20岁精致年轻女生，精致五官，杏眼，高颅顶黑长直，头发柔顺有光泽，身穿简约浅杏色连衣裙
将7-9秒女生换成20岁精致东亚女生，乌黑低盘发，少量碎发，身穿浅灰色简约吊带
将9-10秒产品背景换成新中式禅意静物场景，前景是大块实木桌面，清晰自然的木头木纹肌理，柔和暖调斜向阳光，光线斜洒在木桌上，形成柔和光影；画面左后方虚化，摆放一盆小型盆景绿植、深色木托盘，米黄色哑光肌理墙面作为背景，浅景深，不改变动作和产品
将10-13秒女生换成20岁精致女生，甜酷气质，浅黑茶色中长发，蓬松有层次，身穿浅蓝色短款衬衫
将14-22秒产品背景换成新中式禅意静物场景，前景是大块实木桌面，清晰自然的木头木纹肌理，柔和暖调斜向阳光，光线斜洒在木桌上，形成柔和光影；画面左后方虚化，摆放一盆小型盆景绿植、深色木托盘，米黄色哑光肌理墙面作为背景，浅景深，不改变动作和产品
将22-24秒女生换成20岁精致女生，对镜自拍，黑色高马尾，留有鬓角碎发，身穿白色简约短袖。
```

注意：原始成功案例的时间段中存在 `13–14 秒` 未明确写修改内容。后续系统不应擅自认为这是错误；如果该时段没有修改，应明确继承原视频，如果是遗漏则交给用户确认。

---

## 18. 来源文件

本报告使用了用户提供的以下资料：

1. `C:\Users\Administrator\.codex\attachments\0116dc54-3ac8-4129-812c-6ee61f4c175b\pasted-text.txt`
   内容：小屋 Seedance 2.5 官方直连配置说明，对应“谢飞的魔法妙妙屋（火山版）”1.6.12。

2. `C:\Users\Administrator\.codex\attachments\34e0c009-2db2-4bff-af96-c5182b280690\pasted-text.txt`
   内容：Seedance 2.5（Comfly）接口配置文档。

这些来源文件不包含真实 API Key。若未来来源文件更新，应更新本文版本号、日期和接口差异，而不是直接覆盖旧结论却不留记录。

---

## 19. 本版本结论

当前已经足以确认业务方向和两家 Seedance 2.5 接口的主要契约，但还不足以开始编码。下一步应先由用户审阅本文，纠正遗漏或误解；随后逐项确认未决问题，再进入独立系统设计。
